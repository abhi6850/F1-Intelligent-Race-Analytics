# ── Python ──────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environments — NEVER commit these
venv/
env/
.env/
.venv/

# ── ML Models — large binary files, exclude from git ──
models/*.pkl
models/*.joblib
models/*.h5
models/*.pt
models/*.bin

# ── Training data — too large for git ──
data/*.csv
data/*.parquet
data/*.feather
data/*.json
src/
training/

# ── Environment files — contain secrets ──
.env
.env.local
.env.production
backend/.env

# ── Frontend ─────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.vite/

# ── Misc ─────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.vscode/settings.json
*.docx
allfiles.txt
about.txt
working.txt
trial.py
F1
