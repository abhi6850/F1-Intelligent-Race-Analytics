[build]
  base    = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

# Redirect all routes to index.html so React Router works on page refresh
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
